version: '3.8'

services:
  gotreasa-berlin-clock-dev:
    container_name: gotreasa-berlin-clock-dev
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/src
      - goss:/goss
    environment:
      NODE_ENV: production
    ports:
      - 9080:9080
    restart: always
    healthcheck:
      test: ['CMD', '/goss/goss', 'validate']
      interval: 10s
      timeout: 10m
      retries: 5
      start_period: 10s
  goss:
    image: aelsabbahy/goss
    container_name: goss-dev
    volumes:
      - goss:/goss
    command: ['tail', '-f', '/dev/null']

volumes:
  goss:
